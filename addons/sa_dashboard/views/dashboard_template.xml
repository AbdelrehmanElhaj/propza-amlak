<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="dashboard_view">
        <html dir="rtl" lang="ar">
            <head>
                <meta charset="utf-8"/>
                <title>لوحة تحكم — إدارة العقارات</title>
                <link rel="stylesheet" href="/web/static/lib/bootstrap/dist/css/bootstrap.css"/>
                <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
                <style>
                    body {
                        background: #f5f7fa;
                        font-family: 'Segoe UI', Tahoma, Arial, sans-serif;
                        direction: rtl;
                        text-align: right;
                        margin: 0;
                        padding: 20px;
                    }
                    .header {
                        max-width: 1400px;
                        margin: 0 auto 20px;
                        display: flex;
                        justify-content: space-between;
                        align-items: center;
                    }
                    .header h1 { margin: 0; color: #1a3a5c; }
                    .header .date { color: #6c757d; font-size: 14px; }

                    .container-main { max-width: 1400px; margin: 0 auto; }

                    .kpi-grid {
                        display: grid;
                        grid-template-columns: repeat(4, 1fr);
                        gap: 16px;
                        margin-bottom: 24px;
                    }
                    .kpi-card {
                        background: white;
                        padding: 20px;
                        border-radius: 12px;
                        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
                        position: relative;
                        overflow: hidden;
                    }
                    .kpi-card::before {
                        content: '';
                        position: absolute;
                        top: 0; right: 0;
                        width: 4px; height: 100%;
                        background: var(--accent);
                    }
                    .kpi-label { font-size: 13px; color: #6c757d; margin-bottom: 8px; }
                    .kpi-value { font-size: 28px; font-weight: 700; color: #1a3a5c; }
                    .kpi-sub { font-size: 12px; color: #6c757d; margin-top: 4px; }

                    .chart-grid {
                        display: grid;
                        grid-template-columns: 2fr 1fr;
                        gap: 16px;
                        margin-bottom: 24px;
                    }
                    .chart-grid-2 {
                        display: grid;
                        grid-template-columns: 1fr 1fr;
                        gap: 16px;
                        margin-bottom: 24px;
                    }
                    .chart-card {
                        background: white;
                        padding: 20px;
                        border-radius: 12px;
                        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
                    }
                    .chart-card h3 { margin: 0 0 16px; color: #1a3a5c; font-size: 16px; }
                    .chart-card canvas { max-height: 300px; }

                    .list-card { background: white; padding: 20px; border-radius: 12px;
                                 box-shadow: 0 2px 8px rgba(0,0,0,0.05); }
                    .list-card h3 { margin: 0 0 16px; color: #1a3a5c; font-size: 16px; }
                    .list-card table { width: 100%; }
                    .list-card td, .list-card th { padding: 10px 8px; border-bottom: 1px solid #eee; }
                    .list-card th { font-size: 12px; color: #6c757d; font-weight: 500; }
                    .badge-warn { background: #fff3cd; color: #856404; padding: 2px 8px; border-radius: 4px; }
                    .badge-danger { background: #f8d7da; color: #721c24; padding: 2px 8px; border-radius: 4px; }
                </style>
            </head>
            <body>
                <div class="header">
                    <div>
                        <h1>📊 لوحة تحكم إدارة العقارات</h1>
                        <div class="date">آخر تحديث: <span t-out="today"/></div>
                    </div>
                </div>

                <div class="container-main">
                    <!-- KPI cards -->
                    <div class="kpi-grid">
                        <div class="kpi-card" style="--accent:#2c5aa0">
                            <div class="kpi-label">إجمالي العقارات</div>
                            <div class="kpi-value"><span t-out="kpis['total_properties']"/></div>
                            <div class="kpi-sub">
                                <span t-out="kpis['occupied']"/> مؤجَّر،
                                <span t-out="kpis['vacant']"/> شاغر
                            </div>
                        </div>
                        <div class="kpi-card" style="--accent:#198754">
                            <div class="kpi-label">عقود سارية</div>
                            <div class="kpi-value"><span t-out="kpis['active_tenancies']"/></div>
                            <div class="kpi-sub">
                                إيراد متوقع: <span t-out="'%.0f' % kpis['monthly_revenue']"/> ر.س/شهر
                            </div>
                        </div>
                        <div class="kpi-card" style="--accent:#dc3545">
                            <div class="kpi-label">دفعات متأخرة</div>
                            <div class="kpi-value"><span t-out="kpis['overdue_count']"/></div>
                            <div class="kpi-sub">
                                <span t-out="'%.0f' % kpis['overdue_amount']"/> ر.س
                            </div>
                        </div>
                        <div class="kpi-card" style="--accent:#ff9800">
                            <div class="kpi-label">عقود تنتهي خلال 60 يوم</div>
                            <div class="kpi-value"><span t-out="kpis['expiring_60d']"/></div>
                            <div class="kpi-sub">للتجديد قبل النهاية</div>
                        </div>
                    </div>

                    <!-- Revenue trend (large) + occupancy (small) -->
                    <div class="chart-grid">
                        <div class="chart-card">
                            <h3>📈 اتجاه الإيرادات (آخر 12 شهر)</h3>
                            <canvas id="revenueChart"></canvas>
                        </div>
                        <div class="chart-card">
                            <h3>🏠 إشغال العقارات</h3>
                            <canvas id="occupancyChart"></canvas>
                        </div>
                    </div>

                    <!-- Maintenance cost + top overdue -->
                    <div class="chart-grid-2">
                        <div class="chart-card">
                            <h3>🔧 تكاليف الصيانة حسب الفئة (آخر 12 شهر)</h3>
                            <canvas id="maintChart"></canvas>
                        </div>
                        <div class="list-card">
                            <h3>⚠️ أكثر 5 مستأجرين تأخراً</h3>
                            <table>
                                <thead>
                                    <tr>
                                        <th>المستأجر</th>
                                        <th style="text-align:left">إجمالي المتأخر</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="top_overdue" t-as="row">
                                        <tr>
                                            <td><span t-out="row[0]"/></td>
                                            <td style="text-align:left">
                                                <span class="badge-danger">
                                                    <span t-out="'%.0f' % row[1]"/> ر.س
                                                </span>
                                            </td>
                                        </tr>
                                    </t>
                                    <tr t-if="not top_overdue">
                                        <td colspan="2" style="text-align:center;color:#6c757d">
                                            لا توجد متأخرات 🎉
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- ═══ CRM KPIs ══════════════════════════════════ -->
                    <div style="border-top:2px solid #e9ecef;margin:8px 0 20px;padding-top:20px">
                        <h2 style="color:#1a3a5c;margin:0 0 16px;font-size:18px">🤝 إدارة علاقات العملاء (CRM)</h2>
                    </div>

                    <div class="kpi-grid" style="margin-bottom:20px">
                        <div class="kpi-card" style="--accent:#7b1fa2">
                            <div class="kpi-label">طلبات مفتوحة</div>
                            <div class="kpi-value"><span t-out="crm_kpis['open_leads']"/></div>
                            <div class="kpi-sub">في خط سير المبيعات</div>
                        </div>
                        <div class="kpi-card" style="--accent:#1565c0">
                            <div class="kpi-label">قيمة الخط الحالي</div>
                            <div class="kpi-value" style="font-size:22px">
                                <span t-out="'%.0f' % crm_kpis['pipeline_value']"/>
                                <span style="font-size:14px;font-weight:400"> ر.س</span>
                            </div>
                            <div class="kpi-sub">مجموع ميزانية الطلبات المفتوحة</div>
                        </div>
                        <div class="kpi-card" style="--accent:#2e7d32">
                            <div class="kpi-label">صفقات فازت هذا الشهر</div>
                            <div class="kpi-value"><span t-out="crm_kpis['won_this_month']"/></div>
                            <div class="kpi-sub">
                                معدل التحويل: <span t-out="crm_kpis['conversion_rate']"/>%
                            </div>
                        </div>
                        <div class="kpi-card" style="--accent:#f57c00">
                            <div class="kpi-label">متوسط أيام الإغلاق</div>
                            <div class="kpi-value"><span t-out="crm_kpis['avg_days_close']"/></div>
                            <div class="kpi-sub">للصفقات المُغلقة (آخر 30 يوم)</div>
                        </div>
                    </div>

                    <!-- CRM Pipeline chart + top agents -->
                    <div class="chart-grid-2" style="margin-bottom:20px">
                        <div class="chart-card">
                            <h3>📊 خط السير — الطلبات المفتوحة حسب المرحلة</h3>
                            <canvas id="pipelineChart"></canvas>
                        </div>
                        <div class="list-card">
                            <h3>🏆 أفضل 5 موظفين (صفقات مُغلقة)</h3>
                            <table>
                                <thead>
                                    <tr>
                                        <th>#</th>
                                        <th>الموظف</th>
                                        <th style="text-align:left">الصفقات</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-set="rank" t-value="0"/>
                                    <t t-foreach="top_agents" t-as="agent">
                                        <t t-set="rank" t-value="rank + 1"/>
                                        <tr>
                                            <td style="color:#6c757d;font-size:12px">
                                                <t t-if="rank == 1">🥇</t>
                                                <t t-elif="rank == 2">🥈</t>
                                                <t t-elif="rank == 3">🥉</t>
                                                <t t-else=""><span t-out="rank"/></t>
                                            </td>
                                            <td><span t-out="agent[0]"/></td>
                                            <td style="text-align:left">
                                                <span style="background:#e8f5e9;color:#2e7d32;padding:2px 10px;border-radius:12px;font-weight:600">
                                                    <span t-out="agent[1]"/>
                                                </span>
                                            </td>
                                        </tr>
                                    </t>
                                    <tr t-if="not top_agents">
                                        <td colspan="3" style="text-align:center;color:#6c757d">
                                            لا توجد صفقات مُغلقة بعد
                                        </td>
                                    </tr>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <!-- Reservations expiring within 7 days -->
                    <t t-if="expiring_res_list">
                        <div class="list-card" style="margin-bottom:24px;border-right:4px solid #dc3545">
                            <h3>🔔 حجوزات تنتهي خلال 7 أيام</h3>
                            <table>
                                <thead>
                                    <tr>
                                        <th>رقم الحجز</th>
                                        <th>العميل</th>
                                        <th>العقار</th>
                                        <th>تاريخ الانتهاء</th>
                                        <th>الأيام المتبقية</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <t t-foreach="expiring_res_list" t-as="res">
                                        <tr>
                                            <td><span t-out="res['name']"/></td>
                                            <td><span t-out="res['partner']"/></td>
                                            <td><span t-out="res['property']"/></td>
                                            <td><span t-out="res['date_end']"/></td>
                                            <td>
                                                <span class="badge-danger">
                                                    <span t-out="res['days_left']"/> يوم
                                                </span>
                                            </td>
                                        </tr>
                                    </t>
                                </tbody>
                            </table>
                        </div>
                    </t>

                    <!-- Expiring contracts -->
                    <div class="list-card" style="margin-bottom:24px">
                        <h3>📅 العقود التي تنتهي قريباً (60 يوم)</h3>
                        <table>
                            <thead>
                                <tr>
                                    <th>رقم العقد</th>
                                    <th>المستأجر</th>
                                    <th>العقار</th>
                                    <th>تاريخ النهاية</th>
                                    <th>الأيام المتبقية</th>
                                </tr>
                            </thead>
                            <tbody>
                                <t t-foreach="expiring_list" t-as="t">
                                    <tr>
                                        <td><span t-out="t['name']"/></td>
                                        <td><span t-out="t['tenant']"/></td>
                                        <td><span t-out="t['property']"/></td>
                                        <td><span t-out="t['end_date']"/></td>
                                        <td>
                                            <span class="badge-warn">
                                                <span t-out="t['days_left']"/> يوم
                                            </span>
                                        </td>
                                    </tr>
                                </t>
                                <tr t-if="not expiring_list">
                                    <td colspan="5" style="text-align:center;color:#6c757d">
                                        لا توجد عقود تنتهي قريباً
                                    </td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Chart data passed via data-* attributes -->
                <div id="chart-data"
                     t-att-data-months="months_json"
                     t-att-data-occupancy="occupancy_json"
                     t-att-data-maint="maint_by_cat_json"
                     t-att-data-pipeline="leads_by_stage_json"
                     style="display:none"/>

                <script>
                    document.addEventListener('DOMContentLoaded', function() {
                        const el = document.getElementById('chart-data');
                        const monthsData = JSON.parse(el.dataset.months);
                        const occupancyData = JSON.parse(el.dataset.occupancy);
                        const maintData = JSON.parse(el.dataset.maint);
                        const pipelineData = JSON.parse(el.dataset.pipeline);

                        // Revenue trend chart
                        new Chart(document.getElementById('revenueChart'), {
                            type: 'line',
                            data: {
                                labels: monthsData.map(d => d.month),
                                datasets: [
                                    {
                                        label: 'المستحق',
                                        data: monthsData.map(d => d.due),
                                        borderColor: '#2c5aa0',
                                        backgroundColor: 'rgba(44,90,160,0.1)',
                                        tension: 0.3,
                                        fill: true,
                                    },
                                    {
                                        label: 'المُسدَّد',
                                        data: monthsData.map(d => d.paid),
                                        borderColor: '#198754',
                                        backgroundColor: 'rgba(25,135,84,0.1)',
                                        tension: 0.3,
                                        fill: true,
                                    }
                                ]
                            },
                            options: {
                                responsive: true,
                                plugins: { legend: { position: 'bottom' } },
                                scales: { y: { beginAtZero: true } }
                            }
                        });

                        // Occupancy donut
                        new Chart(document.getElementById('occupancyChart'), {
                            type: 'doughnut',
                            data: {
                                labels: ['مؤجَّر', 'شاغر', 'أخرى'],
                                datasets: [{
                                    data: [occupancyData.on_rent, occupancyData.vacant, occupancyData.other],
                                    backgroundColor: ['#198754', '#ff9800', '#6c757d'],
                                }]
                            },
                            options: {
                                responsive: true,
                                plugins: { legend: { position: 'bottom' } }
                            }
                        });

                        // Maintenance cost bar
                        const maintLabels = Object.keys(maintData);
                        const maintValues = Object.values(maintData);
                        new Chart(document.getElementById('maintChart'), {
                            type: 'bar',
                            data: {
                                labels: maintLabels,
                                datasets: [{
                                    label: 'التكلفة (ر.س)',
                                    data: maintValues,
                                    backgroundColor: '#f57c00',
                                }]
                            },
                            options: {
                                responsive: true,
                                plugins: { legend: { display: false } },
                                scales: { y: { beginAtZero: true } }
                            }
                        });

                        // CRM pipeline funnel (horizontal bar)
                        if (pipelineData.length) {
                            new Chart(document.getElementById('pipelineChart'), {
                                type: 'bar',
                                data: {
                                    labels: pipelineData.map(d => d.stage),
                                    datasets: [{
                                        label: 'عدد الطلبات',
                                        data: pipelineData.map(d => d.count),
                                        backgroundColor: [
                                            '#1565c0','#1976d2','#42a5f5',
                                            '#7b1fa2','#9c27b0','#ce93d8',
                                        ],
                                        borderRadius: 4,
                                    }]
                                },
                                options: {
                                    indexAxis: 'y',
                                    responsive: true,
                                    plugins: { legend: { display: false } },
                                    scales: { x: { beginAtZero: true, ticks: { stepSize: 1 } } }
                                }
                            });
                        }
                    });
                </script>
            </body>
        </html>
    </template>

</odoo>
